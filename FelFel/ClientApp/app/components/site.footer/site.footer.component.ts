import { Component } from "@angular/core";

@Component({
    selector: "footer",
    template: require("./site.footer.component.html")
})

export class SiteFooterComponent {
    constructor(
    ) {
    }
}