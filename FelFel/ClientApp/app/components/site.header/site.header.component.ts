import { Component } from "@angular/core";

@Component({
    selector: "header",
    template: require("./site.header.component.html")
})

export class SiteHeaderComponent {
    constructor(
    ) {
    }
}