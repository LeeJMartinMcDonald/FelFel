import { Component } from "@angular/core";

@Component({
    selector: "site-menu",
    template: require("./site.menu.component.html")
})

export class SiteMenuComponent {
    constructor(
    ) {
    }
}